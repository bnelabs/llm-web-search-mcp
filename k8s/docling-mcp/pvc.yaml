apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docling-models
  namespace: docling-mcp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 5Gi
